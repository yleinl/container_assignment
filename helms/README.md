Install with helm chart:

In this directory,
```bash
helm install <name> .
```

Uninstall with helm chart:
```bash
helm uninstall <name>
```

Upgrade with helm chart:
```bash
helm upgrade <name>
```

See release list:
```bash
helm list
```

