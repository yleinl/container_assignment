apiVersion: v1
 kind: Service
 metadata:
   name: mysql-deploy
 spec:
   ports:
     - name: mysql-deploy-port
       protocol: TCP
       port: 3306
       targetPort: 3306
      #  nodePort: 33306
   type: ClusterIP
   selector:
     app: mysql-deploy
